# Project Rules

- **Branching Strategy**: Before working on any new feature, always create a new branch named after the feature category (e.g., `feature/auth`, `feature/devops`) from the main branch. Only commit and push work on these feature branches to facilitate a PR-based Git workflow.
